<!doctype html>
<html lang="en">
<head>
    <%- include("partials/head", {title: "Veille d'examen BTS SIO SLAM"}) %>
    <link rel="stylesheet" href="/stylesheets/veille-style.css">

    <script type="module" defer src="/javascripts/main-animator.min.js"></script>
</head>
<body>

<%- include("partials/header", {pageHeader: "Veille technologique", pageHeaderShort: "Veille", route:"/veille"}) %>
<main class="home-section">
    <h1>Veille Technologique</h1>

    <h2>Docker est-il l'avenir du déploiement ? (OUI)</h2>

    <h3>Articles à ce propos</h3>
    <div class="cards__main-container">
        <% articles.forEach(article => {%>
            <div class="card__container card__category__<%= article.categories %>">
                <div class="card__image" style="background-image: url('/images/articles/<%= article.img %>')"></div>
                <div class="card__body">
                    <p class="card__title"><%= article.title %></p>
                    <p class="card__text"><%= article.description %></p>
                </div>
                <a class="card__link" href="/articles/view/<%= article.slug %>"></a>
            </div>
        <% }) %>
    </div>

</main>

<%- include("partials/footer") %>

</body>
</html>
